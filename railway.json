{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "sh -c 'if [ -n \"$FIREBASE_SERVICE_ACCOUNT_JSON\" ]; then echo \"$FIREBASE_SERVICE_ACCOUNT_JSON\" > firebase-service-account.json; fi && python -m uvicorn main:app --host 0.0.0.0 --port $PORT'",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}